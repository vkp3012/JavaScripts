let num = 0;
console.log(num++);
console.log(++num);
console.log(num);


// The postfix unary operator ++:

// Returns the value (this returns 0)

// Increments the value (number is now 1)
// The prefix unary operator ++:

// Increments the value (number is now 2)
// Returns the value (this returns 2)
// This returns 0 2 2.


console.log("------------------------------------");

const sum = eval("10*10+5")
console.log(sum);

// eval evaluates codes that's passed as a string. 
// If it's an expression, like in this case, it evaluates the expression. 
// The expression is 10 * 10 + 5. This returns the number 105.